export type project = {
  name: string;
  description: string;
  href: string;
  img?: string;
  className?: string;
};

export type skill = {
  name: string;
  description: string;
  icon?: JSX.Element;
  className?: string;
};
